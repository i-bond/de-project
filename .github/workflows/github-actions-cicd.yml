# This is a basic workflow to help you get started with Actions
name: Deploy to VM

# Controls when the action will run.
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches:
      - main
    paths-ignore: # Don't trigger on files that are updated by the CI
      - "README.md"


# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "autoupdate"
  autoupdate:
    # The type of runner that the job will run on
    runs-on: ubuntu-20.04

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: Remote SSH Commands
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username:  ${{ secrets.USER }}
          key: ${{ secrets.SSH_KEY }}
          port: 22
          script: | # multi line script
                  ls
                  cd de-project
                  echo "Fetching the remote branch and moving it to origin/main"
                  git fetch origin main
                  git reset --hard origin/main
                  git status

                  echo "${{ secrets.NAME }} ${{ secrets.KEY }}" > file_test.txt
                  echo "Updating Docker Containers"
                  sudo docker-compose down --volumes --rmi all
                  sudo docker-compose build --build-arg ${{ secrets.NAME }} --build-arg ${{ secrets.KEY }}
                  echo "We are Up and Running!"
#                  sudo docker-compose up -d

#                  Provide in Terraform, json doesn't work as well as .env
#                  # Add GCP credentials -
#                  CRED_DIR=~/de-project/data_pipeline/de_airflow/credentials
#                  if [ -d "$CRED_DIR" ]; then
#                    echo "$CRED_DIR exists."
#                  else
#                    echo "$CRED_DIR does not exist, creating ..."
#                    mkdir $CRED_DIR
#                    echo "${{ secrets.GOOGLE_CREDENTIALS }}" > $CRED_DIR/google_credentials.json
#                  fi


























